services:
  gateway:
    build: .
    image: ${OPL_FLEET_COCKPIT_LOCAL_IMAGE:-opl-fleet-cockpit:local}
    network_mode: bridge
    ports:
      - "${OPL_FLEET_COCKPIT_PORT:-${AMBIENT_OPS_PORT:-8787}}:8787"
    environment:
      DISCOVERY_ENABLED: "false"
